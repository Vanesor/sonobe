# FL-ZKP System Architecture Diagrams

## System Overview

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    FL-ZKP Bridge Architecture                            │
└─────────────────────────────────────────────────────────────────────────┘

┌──────────────────────────┐
│   Python Layer (PyO3)    │
├──────────────────────────┤
│  • FLZKPProver class     │
│  • initialize()          │
│  • prove_gradient_step() │
│  • generate_proof()      │
│  • verify_proof()        │
└────────────┬─────────────┘
             │ PyO3 Bindings
             ▼
┌──────────────────────────┐
│   Rust Core (lib.rs)     │
├──────────────────────────┤
│  • AdditionFCircuit      │
│  • ProtoGalaxy folding   │
│  • Field conversions     │
│  • Proof serialization   │
└────────────┬─────────────┘
             │ Uses
             ▼
┌──────────────────────────┐
│   Sonobe Library         │
├──────────────────────────┤
│  • Nova IVC              │
│  • CycleFold             │
│  • Commitment schemes    │
│  • Decider (Groth16)     │
└──────────────────────────┘
```

## Federated Learning Workflow

```
Step 1: Initialize
──────────────────
┌───────────┐
│ FL Server │
└─────┬─────┘
      │ prover = FLZKPProver()
      │ prover.initialize(0.0)
      ▼
┌────────────────────┐
│ ZKP System Ready   │
│ Initial state: 0.0 │
└────────────────────┘


Step 2: Client Training
────────────────────────
┌──────────┐  ┌──────────┐  ┌──────────┐
│ Client 1 │  │ Client 2 │  │ Client 3 │
└────┬─────┘  └────┬─────┘  └────┬─────┘
     │             │             │
     │ Train on    │ Train on    │ Train on
     │ local data  │ local data  │ local data
     │             │             │
     ▼             ▼             ▼
  grad=0.5      grad=-0.3     grad=0.7


Step 3: Proving Gradients
──────────────────────────
     │             │             │
     └─────────────┼─────────────┘
                   ▼
         ┌─────────────────┐
         │  ZKP Prover     │
         │  ┌───────────┐  │
         │  │ Step 1    │  │  prove_gradient_step(0.5)
         │  │ z=0+0.5   │  │  ──────────────────────────>
         │  │ =0.5  ✓   │  │  state: 0.5, proof: P1
         │  └───────────┘  │
         │  ┌───────────┐  │
         │  │ Step 2    │  │  prove_gradient_step(-0.3)
         │  │ z=0.5-0.3 │  │  ──────────────────────────>
         │  │ =0.2  ✓   │  │  state: 0.2, proof: P2
         │  └───────────┘  │
         │  ┌───────────┐  │
         │  │ Step 3    │  │  prove_gradient_step(0.7)
         │  │ z=0.2+0.7 │  │  ──────────────────────────>
         │  │ =0.9  ✓   │  │  state: 0.9, proof: P3
         │  └───────────┘  │
         └─────────────────┘


Step 4: IVC Proof
─────────────────
         ┌─────────────────┐
         │  ZKP Prover     │
         │                 │
         │  generate_final │  ┌──────────────────┐
         │  _proof()       │──>│ ProtoGalaxy IVC  │
         │                 │  │ Accumulated proof│
         │  ✓ Folded!      │  │ Proves:          │
         └─────────────────┘  │ - 3 gradients    │
                              │ - Correct sum    │
                              │ - Final: 0.9     │
                              └──────────────────┘


Step 5: Verification
────────────────────
┌──────────────────┐       ┌──────────┐
│ IVC Proof        │──────>│ Verifier │
│ (accumulated)    │       └────┬─────┘
└──────────────────┘            │
                                │ verify_proof()
                                │ ~5-10ms
                                ▼
                        ┌───────────────┐
                        │ ✓ VALID       │
                        │ Aggregation   │
                        │ is correct!   │
                        └───────────────┘
```

## Circuit Constraint Flow

```
Addition Circuit: z_{i+1} = z_i + gradient
──────────────────────────────────────────

Input:
  z_i (current state)         ────┐
  gradient (external input)   ────┤
                                  │
                                  ▼
                        ┌──────────────────┐
                        │  R1CS Constraint │
                        │                  │
                        │  z_{i+1} =       │
                        │  z_i + gradient  │
                        │                  │
                        └────────┬─────────┘
                                 │
                                 ▼
Output:
  z_{i+1} (next state)      ────┘


ProtoGalaxy Folding (Incremental Verification):
───────────────────────────────────────────────

Initial:  z_0 = 0.0
          ↓
Step 1:   z_1 = z_0 + grad_1 = 0.0 + 0.5 = 0.5     [Fold proof]
          ↓
Step 2:   z_2 = z_1 + grad_2 = 0.5 - 0.3 = 0.2     [Fold proof]
          ↓
Step 3:   z_3 = z_2 + grad_3 = 0.2 + 0.7 = 0.9     [Fold proof]
          ↓
Final:    IVC verification of accumulated instances
          ↓
          Proof: Initial=0.0, Final=0.9, Steps=3 ✓
```

## Cryptographic Stack

```
┌────────────────────────────────────────────────────────────┐
│                      Proof Layer                            │
│                                                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │           ProtoGalaxy IVC (Direct)                   │  │
│  │  • Accumulated instances verification                │  │
│  │  • Fast verification: ~5-10ms                        │  │
│  │  • Proves all folding steps correct                  │  │
│  └──────────────────────────────────────────────────────┘  │
└──────────────────────┬─────────────────────────────────────┘
                       │
┌──────────────────────┴─────────────────────────────────────┐
│                Folding Layer (ProtoGalaxy)                  │
│                                                             │
│  ┌────────────────────────────┐  ┌──────────────────────┐  │
│  │   Primary Curve (BN254)    │  │  Secondary Curve     │  │
│  │   • Main computation       │  │  (Grumpkin)          │  │
│  │   • Pedersen commits       │  │  • CycleFold         │  │
│  │   • F = Scalar field       │  │  • Pedersen commits  │  │
│  └────────────────────────────┘  └──────────────────────┘  │
│                                                             │
│  Incremental Verification:                                  │
│  • Each step folds previous proof                          │
│  • Constant-size accumulated proof                         │
│  • Linear time in number of steps                          │
└─────────────────────────────────────────────────────────────┘
                       │
┌──────────────────────┴─────────────────────────────────────┐
│                    Circuit Layer                            │
│                                                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │           Addition Circuit (R1CS)                    │  │
│  │                                                      │  │
│  │  Constraint: z_{i+1} = z_i + gradient                │  │
│  │                                                      │  │
│  │  Variables:                                          │  │
│  │  • z_i:       State variable (field element)         │  │
│  │  • gradient:  External input (field element)         │  │
│  │  • z_{i+1}:   Next state (witness)                   │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

## Performance Characteristics

```
Time Complexity by Operation:
──────────────────────────────

Initialization:     O(1) - ~3-5 seconds
                    │
                    └─> One-time setup per FL round

Per-Step Proving:   O(n) - ~10-50ms per gradient
                    │
                    └─> Linear in number of gradients
                        100 gradients = ~3 seconds

Final Proof:        O(1) - ~5-10 seconds
                    │
                    └─> Constant time (Groth16 proving)

Verification:       O(1) - ~5-10ms
                    │
                    └─> Very fast, constant time


Proof Size:
───────────

Incremental:  Growing with steps (internal)
              │
              └─> Not transmitted

Final:        ~500 bytes (Groth16)
              │
              └─> Succinct, constant size!


Total Time for 100 Clients:
────────────────────────────

Init:     5s    █████
Proving:  3s    ███
Proof:    8s    ████████
         ───    ────────────────
Total:   16s    ████████████████
```

## Privacy Model

```
┌─────────────────────────────────────────────────────────────┐
│                    Privacy Guarantees                        │
└─────────────────────────────────────────────────────────────┘

What is Public:
───────────────
  ✓ Initial state (z_0)
  ✓ Final state (z_n)
  ✓ Number of steps (n)
  ✓ Circuit description

                    ┌──────────────┐
                    │  Public View │
                    ├──────────────┤
                    │ z_0 = 0.0    │
                    │ ...          │
                    │ z_n = 0.9    │
                    │ steps = 3    │
                    └──────────────┘


What is Hidden:
───────────────
  🔒 Individual gradients (g_1, g_2, g_3)
  🔒 Intermediate states (z_1, z_2)
  🔒 Client identities
  🔒 Computation details

                    ┌──────────────┐
                    │ Hidden Data  │
                    ├──────────────┤
                    │ g_1 = 0.5   🔒│
                    │ g_2 = -0.3  🔒│
                    │ g_3 = 0.7   🔒│
                    │ z_1 = 0.5   🔒│
                    │ z_2 = 0.2   🔒│
                    └──────────────┘


Zero-Knowledge Property:
────────────────────────
Proof reveals nothing beyond:
  "The final state is correctly computed from initial state"

Even a malicious verifier cannot learn individual gradients!
```

## Comparison with Traditional FL

```
Traditional FL (No ZKP):
────────────────────────

Client 1 ──> gradient_1 = 0.5   ──┐
                                  │
Client 2 ──> gradient_2 = -0.3  ──┤──> Server sees all gradients
                                  │    • Privacy leak!
Client 3 ──> gradient_3 = 0.7   ──┘    • Must trust server


FL with ZKP (This System):
──────────────────────────

Client 1 ──> gradient_1 ────┐
                            │
Client 2 ──> gradient_2 ────┤──> ZKP Prover
                            │    │
Client 3 ──> gradient_3 ────┘    │
                                 ▼
                         ┌───────────────┐
                         │ Proof (public)│
                         │ Size: 500B    │
                         │ Hides grads!  │
                         └───────┬───────┘
                                 │
                                 ▼
                         ┌───────────────┐
                         │ Verifier      │
                         │ ✓ Correct sum │
                         │ 🔒 No leak    │
                         └───────────────┘

Benefits:
  ✓ Privacy preserved
  ✓ Verifiable correctness
  ✓ No trust required
  ✓ Cryptographic guarantee
```

---

## Legend

```
┌─────┐  Box: Component/Module
│     │
└─────┘

  ───>   Flow/Arrow: Data or control flow

  ✓      Checkmark: Success/Valid
  ✗      Cross: Failure/Invalid
  🔒     Lock: Private/Hidden
  ⚠      Warning: Caution

┌───────┐
│ Text  │  Descriptive information
└───────┘
```
